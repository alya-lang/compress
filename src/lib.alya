## Compress - Comprehensive Data Compression Toolkit for Alya
##
## Part of the Alya Language package ecosystem (https://github.com/alya-lang).
##
## Provides high-performance compression codecs, checksum algorithms, archive
## handling, and unified format dispatching.
##
## ### Supported Algorithms
## - **Brotli** (RFC 7932)
## - **Bzip2** (Burrows-Wheeler block sorting)
## - **DEFLATE** (RFC 1951 raw deflate)
## - **Gzip** (RFC 1952 deflate with gzip headers and CRC-32)
## - **LZ Family** (LZ4, LZ77, LZ78, LZSS, LZW, LZMA, LZMA2, LZJB)
## - **Google Snappy** (framing and raw block compression)
## - **SZIP / ZIP** (in-memory zip and disk archive manipulation)
## - **Zlib** (RFC 1950 deflate with zlib wrapper and Adler-32)
## - **Zstandard** (Scalable real-time compression)
## - **Huffman** (Optimal prefix canonical coder)
## - **Checksums** (CRC-32, Adler-32)

import "types.alya"
import "deflate.alya"
import "zlib.alya"
import "gzip.alya"
import "lz4.alya"
import "lz.alya"
import "snappy.alya"
import "zstd.alya"
import "bzip2.alya"
import "brotli.alya"
import "szip.alya"
import "checksum.alya"
import "huffman.alya"

# ═══════════════════════════════════════════════════════════════
# Unified Format Dispatch
# ═══════════════════════════════════════════════════════════════

## Compresses input data using the specified compression format and level.
##
## ### Parameters
## - `data`: Input byte array or string.
## - `format`: Format identifier (`CompressionFormat` enum or string name).
## - `level`: Compression level preset (`CompressionLevel` or integer).
##
## ### Returns
## Compressed byte array.
pub function compress_by_format(data, format = CompressionFormat.Gzip, level = CompressionLevel.Default) -> array
    when format
        is CompressionFormat.Deflate, "deflate" then return deflate_level(data, level)
        is CompressionFormat.Zlib, "zlib" then return zlib_level(data, level)
        is CompressionFormat.Gzip, "gzip" then return gzip_level(data, level)
        is CompressionFormat.Lz4, "lz4" then return lz4(data)
        is CompressionFormat.Snappy, "snappy" then return snappy(data)
        is CompressionFormat.Brotli, "brotli" then return brotli_quality(data, level)
        is CompressionFormat.Bzip2, "bzip2" then return bzip2_level(data, level)
        is CompressionFormat.Zstd, "zstd" then return zstd_level(data, level)
        is CompressionFormat.Huffman, "huffman" then return huffman(data)
        is CompressionFormat.Szip, "szip" then return szip(data)
        is CompressionFormat.Lz77, "lz77" then return lz_compress(data, 0)
        is CompressionFormat.Lz78, "lz78" then return lz_compress(data, 1)
        is CompressionFormat.Lzw, "lzw" then return lz_compress(data, 2)
        is CompressionFormat.Lzss, "lzss" then return lz_compress(data, 4)
        is CompressionFormat.Lzma, "lzma" then return lz_compress(data, 5)
        is CompressionFormat.Lzma2, "lzma2" then return lz_compress(data, 6)
        is CompressionFormat.Lzjb, "lzjb" then return lz_compress(data, 7)
        else return gzip(data)
    end
end

## Decompresses data using the specified compression format.
##
## ### Parameters
## - `data`: Compressed byte array.
## - `format`: Format identifier (`CompressionFormat` enum or string name).
##
## ### Returns
## Decompressed byte array.
pub function decompress_by_format(data, format = CompressionFormat.Gzip) -> array
    when format
        is CompressionFormat.Deflate, "deflate" then return inflate(data)
        is CompressionFormat.Zlib, "zlib" then return unzlib(data)
        is CompressionFormat.Gzip, "gzip" then return gunzip(data)
        is CompressionFormat.Lz4, "lz4" then return unlz4(data)
        is CompressionFormat.Snappy, "snappy" then return unsnappy(data)
        is CompressionFormat.Brotli, "brotli" then return unbrotli(data)
        is CompressionFormat.Bzip2, "bzip2" then return unbzip2(data)
        is CompressionFormat.Zstd, "zstd" then return unzstd(data)
        is CompressionFormat.Huffman, "huffman" then return bytes_from_string(unhuffman(data))
        is CompressionFormat.Szip, "szip" then return unszip(data)
        is CompressionFormat.Lz77, "lz77" then return lz_decompress(data, 0)
        is CompressionFormat.Lz78, "lz78" then return lz_decompress(data, 1)
        is CompressionFormat.Lzw, "lzw" then return lz_decompress(data, 2)
        is CompressionFormat.Lzss, "lzss" then return lz_decompress(data, 4)
        is CompressionFormat.Lzma, "lzma" then return lz_decompress(data, 5)
        is CompressionFormat.Lzma2, "lzma2" then return lz_decompress(data, 6)
        is CompressionFormat.Lzjb, "lzjb" then return lz_decompress(data, 7)
        else return gunzip(data)
    end
end

## Computes a checksum for a byte array using CRC-32 or Adler-32.
##
## ### Parameters
## - `bytes`: Byte array integers `[0..255]`.
## - `algorithm`: Checksum algorithm (`ChecksumAlgorithm` enum or string `"crc32"` / `"adler32"`).
##
## ### Returns
## 32-bit unsigned integer checksum.
pub function compute_checksum(bytes, algorithm = ChecksumAlgorithm.Crc32) -> int
    when algorithm
        is ChecksumAlgorithm.Adler32, "adler32" then return adler32(bytes)
        else return crc32(bytes)
    end
end

## Computes a checksum for a text string using CRC-32 or Adler-32.
##
## ### Parameters
## - `text`: Input text string.
## - `algorithm`: Checksum algorithm (`ChecksumAlgorithm` enum or string `"crc32"` / `"adler32"`).
##
## ### Returns
## 32-bit unsigned integer checksum.
pub function compute_checksum_str(text: str, algorithm = ChecksumAlgorithm.Crc32) -> int
    when algorithm
        is ChecksumAlgorithm.Adler32, "adler32" then return adler32_str(text)
        else return crc32_str(text)
    end
end

# ═══════════════════════════════════════════════════════════════
# 1. Brotli (RFC 7932)
# ═══════════════════════════════════════════════════════════════

## Compresses a byte array using Brotli (default quality 11).
pub function brotli(bytes) -> array
    return brotli_compress(bytes)
end

## Compresses a string using Brotli (default quality 11).
pub function brotli_str(text: str) -> array
    return brotli_compress_str(text)
end

## Compresses a byte array using Brotli with specific quality (0..11).
pub function brotli_quality(bytes, quality: int) -> array
    return brotli_compress_level(bytes, quality)
end

## Compresses a string using Brotli with specific quality (0..11).
pub function brotli_str_quality(text: str, quality: int) -> array
    return brotli_compress_str_level(text, quality)
end

## Decompresses Brotli data into a byte array.
pub function unbrotli(bytes) -> array
    return brotli_decompress(bytes)
end

## Decompresses Brotli data directly into a UTF-8 string.
pub function unbrotli_str(bytes) -> str
    return brotli_decompress_str(bytes)
end

# ═══════════════════════════════════════════════════════════════
# 2. Bzip2 (Burrows-Wheeler)
# ═══════════════════════════════════════════════════════════════

## Compresses a byte array using Bzip2 (default 900k block size).
pub function bzip2(bytes) -> array
    return bzip2_compress(bytes)
end

## Compresses a string using Bzip2 (default 900k block size).
pub function bzip2_str(text: str) -> array
    return bzip2_compress_str(text)
end

## Compresses a byte array using Bzip2 with specific block size (1..9).
pub function bzip2_block(bytes, block_100k: int) -> array
    return bzip2_compress_level(bytes, block_100k)
end

## Compresses a string using Bzip2 with specific block size (1..9).
pub function bzip2_str_block(text: str, block_100k: int) -> array
    return bzip2_compress_str_level(text, block_100k)
end

## Alias for `bzip2_block`.
pub function bzip2_level(bytes, level: int) -> array
    return bzip2_compress_level(bytes, level)
end

## Alias for `bzip2_str_block`.
pub function bzip2_str_level(text: str, level: int) -> array
    return bzip2_compress_str_level(text, level)
end

## Decompresses Bzip2 data into a byte array.
pub function unbzip2(bytes) -> array
    return bzip2_decompress(bytes)
end

## Decompresses Bzip2 data directly into a UTF-8 string.
pub function unbzip2_str(bytes) -> str
    return bzip2_decompress_str(bytes)
end

# ═══════════════════════════════════════════════════════════════
# 3. DEFLATE (RFC 1951)
# ═══════════════════════════════════════════════════════════════

## Compresses a byte array using raw DEFLATE (default level 6).
pub function deflate(bytes) -> array
    return deflate_compress(bytes)
end

## Compresses a string using raw DEFLATE (default level 6).
pub function deflate_str(text: str) -> array
    return deflate_compress_str(text)
end

## Compresses a byte array using raw DEFLATE with specified level (1..9).
pub function deflate_level(bytes, level: int) -> array
    return deflate_compress_level(bytes, level)
end

## Compresses a string using raw DEFLATE with specified level (1..9).
pub function deflate_str_level(text: str, level: int) -> array
    return deflate_compress_str_level(text, level)
end

## Decompresses raw DEFLATE data into a byte array.
pub function inflate(bytes) -> array
    return deflate_decompress(bytes)
end

## Decompresses raw DEFLATE data directly into a UTF-8 string.
pub function inflate_str(bytes) -> str
    return deflate_decompress_str(bytes)
end

# ═══════════════════════════════════════════════════════════════
# 4. Gzip (RFC 1952)
# ═══════════════════════════════════════════════════════════════

## Compresses a byte array using Gzip (default level 6).
pub function gzip(bytes) -> array
    return gzip_compress(bytes)
end

## Compresses a string using Gzip (default level 6).
pub function gzip_str(text: str) -> array
    return gzip_compress_str(text)
end

## Compresses a byte array using Gzip with specified level (1..9).
pub function gzip_level(bytes, level: int) -> array
    return gzip_compress_level(bytes, level)
end

## Compresses a string using Gzip with specified level (1..9).
pub function gzip_str_level(text: str, level: int) -> array
    return gzip_compress_str_level(text, level)
end

## Decompresses Gzip data into a byte array.
pub function gunzip(bytes) -> array
    return gzip_decompress(bytes)
end

## Decompresses Gzip data directly into a UTF-8 string.
pub function gunzip_str(bytes) -> str
    return gzip_decompress_str(bytes)
end

# ═══════════════════════════════════════════════════════════════
# 5. LZ Family (LZ4)
# ═══════════════════════════════════════════════════════════════

## Compresses a byte array using LZ4.
pub function lz4(bytes) -> array
    return lz4_compress(bytes)
end

## Compresses a string using LZ4.
pub function lz4_str(text: str) -> array
    return lz4_compress_str(text)
end

## Decompresses LZ4 data into a byte array.
pub function unlz4(bytes) -> array
    return lz4_decompress(bytes)
end

## Decompresses LZ4 data directly into a UTF-8 string.
pub function unlz4_str(bytes) -> str
    return lz4_decompress_str(bytes)
end

# ═══════════════════════════════════════════════════════════════
# 6. Snappy
# ═══════════════════════════════════════════════════════════════

## Compresses a byte array using Google Snappy.
pub function snappy(bytes) -> array
    return snappy_compress(bytes)
end

## Compresses a string using Google Snappy.
pub function snappy_str(text: str) -> array
    return snappy_compress_str(text)
end

## Decompresses Snappy data into a byte array.
pub function unsnappy(bytes) -> array
    return snappy_decompress(bytes)
end

## Decompresses Snappy data directly into a UTF-8 string.
pub function unsnappy_str(bytes) -> str
    return snappy_decompress_str(bytes)
end

# ═══════════════════════════════════════════════════════════════
# 7. SZIP
# ═══════════════════════════════════════════════════════════════

## Compresses a byte array using in-memory SZIP / Deflate.
pub function szip(bytes) -> array
    return szip_compress(bytes)
end

## Compresses a string using in-memory SZIP / Deflate.
pub function szip_str(text: str) -> array
    return szip_compress_str(text)
end

## Decompresses SZIP data into a byte array.
pub function unszip(bytes) -> array
    return szip_decompress(bytes)
end

## Decompresses SZIP data directly into a UTF-8 string.
pub function unszip_str(bytes) -> str
    return szip_decompress_str(bytes)
end

# ═══════════════════════════════════════════════════════════════
# 8. Zlib (RFC 1950)
# ═══════════════════════════════════════════════════════════════

## Compresses a byte array using Zlib (default level 6).
pub function zlib(bytes) -> array
    return zlib_compress(bytes)
end

## Compresses a string using Zlib (default level 6).
pub function zlib_str(text: str) -> array
    return zlib_compress_str(text)
end

## Compresses a byte array using Zlib with specified level (1..9).
pub function zlib_level(bytes, level: int) -> array
    return zlib_compress_level(bytes, level)
end

## Compresses a string using Zlib with specified level (1..9).
pub function zlib_str_level(text: str, level: int) -> array
    return zlib_compress_str_level(text, level)
end

## Decompresses Zlib data into a byte array.
pub function unzlib(bytes) -> array
    return zlib_decompress(bytes)
end

## Decompresses Zlib data directly into a UTF-8 string.
pub function unzlib_str(bytes) -> str
    return zlib_decompress_str(bytes)
end

# ═══════════════════════════════════════════════════════════════
# 9. Zstandard (ZSTD)
# ═══════════════════════════════════════════════════════════════

## Compresses a byte array using Zstandard (default level 3).
pub function zstd(bytes) -> array
    return zstd_compress(bytes)
end

## Compresses a string using Zstandard (default level 3).
pub function zstd_str(text: str) -> array
    return zstd_compress_str(text)
end

## Compresses a byte array using Zstandard with specified level (1..22).
pub function zstd_level(bytes, level: int) -> array
    return zstd_compress_level(bytes, level)
end

## Compresses a string using Zstandard with specified level (1..22).
pub function zstd_str_level(text: str, level: int) -> array
    return zstd_compress_str_level(text, level)
end

## Decompresses Zstandard data into a byte array.
pub function unzstd(bytes) -> array
    return zstd_decompress(bytes)
end

## Decompresses Zstandard data directly into a UTF-8 string.
pub function unzstd_str(bytes) -> str
    return zstd_decompress_str(bytes)
end

# ═══════════════════════════════════════════════════════════════
# 10. Huffman Coding
# ═══════════════════════════════════════════════════════════════

## Compresses a string using canonical Huffman prefix coding.
pub function huffman(text: str) -> array
    return huffman_compress(text)
end

## Compresses a string using canonical Huffman prefix coding.
pub function huffman_str(text: str) -> array
    return huffman_compress(text)
end

## Decompresses Huffman data into a string.
pub function unhuffman(bytes) -> str
    return huffman_decompress(bytes)
end

## Decompresses Huffman data directly into a UTF-8 string.
pub function unhuffman_str(bytes) -> str
    return huffman_decompress(bytes)
end
