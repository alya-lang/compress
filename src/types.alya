# Types and data structures for compress
# Part of the Alya Language package ecosystem (https://github.com/alya-lang)

struct CompressConfig
    name
    prefix
    count
end

function make_config(name = "World", count = 1)
    return CompressConfig {
        name: name,
        prefix: "Hello",
        count: count
    }
end
